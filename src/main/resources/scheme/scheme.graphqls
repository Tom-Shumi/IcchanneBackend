type Thread {
    id: ID
    title: String
    url: String
    siteName: String
    category: String
    publishedDate: String
}

type Query {
    getNewThreadList(search:String = null, next: String = null): [Thread]
}